import {
  calculate,
  threshold,
  initWasm,
  initWasmSync
  // @ts-ignore
} from "./random_number_calculator.esm.js";

export { calculate, threshold, initWasm, initWasmSync };

export default {
  calculate,
  threshold,
  initWasm,
  initWasmSync
};
